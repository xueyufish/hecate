"""hecate-sandbox — sandbox / browser / environment domain.

The agent runtime execution surface: Docker-isolated command execution
(sandbox), agent filesystem abstraction (environment), headless browser
sessions (browser). Extracted from the Hecate core package as part of
the package-split plan (PR4c). Import directly from submodules; this
__init__ intentionally exports nothing.
"""
