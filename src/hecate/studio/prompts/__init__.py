"""Studio prompts — prompt management and analytics.

Two files:

- ``service.py`` — prompt CRUD + versioning (relocated from
  ``services/prompt_service.py``).
- ``analytics.py`` — version diff + metrics aggregation
  (relocated from ``services/prompt_analytics_service.py``).
"""

from __future__ import annotations
